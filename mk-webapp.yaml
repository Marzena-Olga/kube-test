apiVersion: apps/v1
kind: Deployment
metadata:
  name: mk-webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mk-webapp
  template:
    metadata:
      labels:
        app: mk-webapp
    spec:
      containers:
      - name: mk-webapp
        image: nginx:1.29.1
        ports:
        - containerPort: 80
